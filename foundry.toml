[profile.default]
src = "src"
out = "out"
libs = ["lib"]
# Pinned: the formal proof covers the bytecode these settings produce.
solc_version = "0.8.37"
optimizer = true
optimizer_runs = 200
via_ir = true
fs_permissions = [{ access = "read", path = "./test/vectors" }]
